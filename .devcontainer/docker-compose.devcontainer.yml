services:
    php:
        build:
            args:
                HOST_UID: ${HOST_UID:-1000}
                HOST_GID: ${HOST_GID:-1000}
        user: "${HOST_UID:-1000}:${HOST_GID:-1000}"
        init: true
